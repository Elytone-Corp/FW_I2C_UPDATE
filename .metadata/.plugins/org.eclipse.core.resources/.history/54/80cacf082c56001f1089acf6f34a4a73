#include "pwm_init.h"
#include "gpio.h"
#include <stdio.h>

int main(void) {
    printf("Starting PWM Capture Program\n");
    pwm_init();

    while (1) {
        // will add it later if required
    }

    return 0;
}
